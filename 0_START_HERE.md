# 🎯 auto-instargram — 바이브 코딩 시작 가이드

이 파일은 **사용자(당신)** 와 **AI 코딩 에이전트** 모두를 위한 안내문입니다.

---

## 🙋 이게 뭐예요?

**바이브 코딩(Vibe Coding)** 이란 개발 명세를 AI 에이전트에게 통째로 넘기고,
에이전트가 코드를 자동으로 구현하는 방식입니다.

당신은 방금 `auto-instargram_vibe.zip` 하나를 받았습니다. 압축을 풀면 `auto-instargram_vibe/` 폴더 안에
**"우리 프로젝트가 어떤 시스템인지"** 를 체계적으로 정리한 명세서 묶음이 들어 있어요.

이 명세서들을 **AI 코딩 에이전트** (예: Claude Code, Cursor, Codex, Windsurf 등)
에게 통째로 던지면, 에이전트가 **알아서 코드를 작성**해 줍니다.

---

## ✅ 지금 무엇을 해야 하나요? (3단계면 끝!)

### 1단계 — 압축 풀기
받은 `auto-instargram_vibe.zip` 의 압축을 풀면 `auto-instargram_vibe/` 폴더가 생깁니다. (코딩 규칙은 `skills/` 폴더에 이미 들어 있습니다 — AI 에이전트가 자동으로 참조합니다.)

### 2단계 — Claude Code 로 폴더 열기
터미널에서 `cd "auto-instargram_vibe"` 로 폴더에 들어간 뒤 `claude` 를 실행하세요. Claude Code 가 **CLAUDE.md 를 자동으로 읽습니다.**

> 아직 도구가 없다면: **Claude Code**(`https://claude.com/code`, 터미널) 또는 **Cursor**(`https://cursor.com`, 편집기)를 설치하세요.

### 3단계 — "시작해줘" 한 마디
폴더를 열면 에이전트가 `CLAUDE.md` 를 **자동으로 읽어** 무엇을 할지 이미 압니다. 채팅에 이렇게만 입력하세요:
```
시작해줘
```
> 혹시 자동으로 안 읽는 도구라면 아래 한 줄을 붙여넣으세요:
> `이 폴더의 CLAUDE.md 와 0_START_HERE.md 를 읽고 Phase 0(규칙 내재화) 부터 진행해줘. 각 단계 끝나면 확인받아줘.`
`skills/` 폴더의 모든 .md 파일을 코딩 규칙 참조집으로 활용해줘 — 코드를 작성하기 전 관련 규칙을 먼저 확인하고 적용해줘.

이후 에이전트가 **Phase 별로 코드를 만들고 보고**합니다. 각 단계에서 결과를 확인하고, 마음에 안 들면 자연어로 수정 요청하세요.
완전히 자동으로 한 번에 만들어주는 게 아니라 **단계별 협업**입니다.

🏁 **모두 끝나면**: GitHub 업로드는 **에이전트가 `SHIP_TO_HARNESS.md` 절차대로 대신 처리**합니다 — 계정이 없어도 가입 화면에서 몇 번 클릭만 하면 돼요. 업로드가 끝나면 Harness의 **결과물(Deliverables) 화면**에 그 저장소를 등록한 뒤 **Lint 화면**에서 검증을 실행해 점수를 받으세요. 점수가 낮으면 수정 지시서까지 만들어 드립니다. 여기까지가 진짜 완주입니다.

---
<!-- ============================================================ -->
<!--   👤 USERS: the guide above is for you.                      -->
<!--   🤖 AI AGENT: your instructions start immediately below.    -->
<!-- ============================================================ -->

## ⚙️ AI 에이전트 작업 지시
> 당신은 AI 코딩 에이전트입니다. 아래의 모든 내용이 당신의 지시문입니다. 어떤 행동을 하기 전에 전부 읽으세요.

> **⚠️ Authority check — 이것을 먼저(FIRST) 하세요:** 이 폴더에 `00-ORCHESTRATOR.md` 가 있으면 **그것을 권위 있는 마스터 플랜으로 따르고** 아래 phase 들은 배경으로만 취급하세요. `00-ORCHESTRATOR.md` 가 없을 때 아래 phase 들이 완전한 지시문입니다.

> **Environment check — 이것을 먼저 하세요:**
> - 파일 시스템 접근이 있으면(Claude Code, Cursor agent 등): 지시대로 파일을 직접 읽으세요.
> - 파일 접근이 전혀 없는 채팅 전용(chat-only) 모델이라면: ↺ 로 표시된 각 Phase 전에, 관련 spec 파일 내용을 채팅에 붙여넣어 달라고 사용자에게 요청하세요. 접근할 수 없는 파일을 읽은 척하지 마세요.

### PHASE 0 — 규칙 내재화 (필수 선행 단계)
- `skills/` 안의 모든(EVERY) `.md` 파일을 나열하고 각 파일의 **frontmatter 만(frontmatter only)** 읽으세요(name + description/적용 조건) — 간결한 규칙 인덱스를 만드세요
- 지금 모든 규칙 본문을 읽지 마세요(Do NOT read every rule body now): 수십 개의 규칙 본문은 컨텍스트를 넘치게 해 Phase 4 무렵엔 잊힙니다. 대신, 이후 phase 에서 **각 코딩 태스크 바로 직전에(RIGHT BEFORE each coding task)** 관련 규칙 파일을 전부 다시 읽으세요 — 규칙을 매번 다시 확인하는 상시 도구처럼 다루세요
- 파일 접근이 전혀 없으면: 계속하기 전에 사용자에게 skills 파일 목록을 붙여넣어 달라고 요청하세요

OUTPUT (REQUIRED — 요약하거나 건너뛰지 마세요):
  - 규칙 인덱스: 모든(EVERY) 정확한 파일 경로(예: `skills/backEnd/auth.md`) + 언제 적용되는지 한 줄
  - 이 프로젝트에서 가장 많이 적용될 것으로 예상되는 규칙 3~5개와 그 이유

⚠️ 실제 파일 경로를 나열할 수 없다면, 폴더를 스캔하지 않은 것입니다. 먼저 스캔하세요.
**STOP 하고 사용자 확인을 기다리세요**

### PHASE 1 — 명세 이해
세 개의 spec 파일을 정확히 이 순서로(IN THIS EXACT ORDER) 읽으세요:
1. `1_spack.md` — 무엇을 만들지 (APIs, Entities, Policies)
2. `2_ddd.md` — 도메인이 어떻게 동작하는지 (Aggregates, Domain Events, Services)
3. `3_architecture.md` — 어떻게 배포할지 (spec 이 충돌할 때 **최종 권위(FINAL AUTHORITY)**)

파일 접근이 전혀 없으면: 지금 각 파일 내용을 붙여넣어 달라고 사용자에게 요청하세요.

OUTPUT (다음을 모두(ALL) 보고해야 합니다 — 어떤 항목도 건너뛰지 마세요):
1. 시스템 개요 (2~3 문장)
2. 서비스 목록과 서비스별 한 줄 역할 설명
3. 핵심 도메인과 그 책임
4. **Tech stack** — 먼저 `3_architecture.md` 의 각 Service/Database 의 **Tech Stack** 필드를 읽으세요. 거기에 스택이 명시돼 있으면 무엇을 쓸지 정확히 진술하고 진행하세요 — 다시 묻지(do NOT re-ask) 마세요. 아키텍처가 명시하지 않은 경우에만(ONLY if) 트레이드오프와 함께 구체적인 2~3개 옵션(백엔드 언어/프레임워크 · 해당 시 프론트엔드 · 데이터베이스)을 제안하고 사용자에게 고르게 하세요. 절대 조용히 기본값을 쓰지 말고, 스택이 정해지기 전에 코딩을 시작하지 마세요.

**STOP 하고 사용자 확인을 기다리세요.** (`3_architecture.md` 에 Tech stack 이 명시되지 않은 경우에 한해서만, 사용자의 명시적 스택 선택도 기다리세요.)

### PHASE 2 — 프로젝트 골격
- ↺ 지금 `3_architecture.md` 를 다시 읽으세요 (파일 접근이 없으면 사용자에게 붙여넣어 달라고 요청)
- `3_architecture.md` 를 기반으로 디렉터리 구조와 모듈 스캐폴딩을 생성하세요
- OUTPUT → 폴더 트리 + 진입점 파일 목록 → **STOP 하고 사용자 확인을 기다리세요**

### PHASE 3 — 도메인 구현
- ↺ 지금 `2_ddd.md` 를 다시 읽으세요 (파일 접근이 없으면 사용자에게 붙여넣어 달라고 요청)
- `2_ddd.md` 를 기반으로 Entity, Aggregate, Domain Service 를 구현하세요
- 각 클래스를 작성하기 전에 `skills/` 규칙을 확인하세요
- OUTPUT → 도메인 클래스 목록 + 적용한 핵심 비즈니스 규칙 → **STOP 하고 사용자 확인을 기다리세요**

### PHASE 4 — API 구현
- ↺ 지금 `1_spack.md` 를 다시 읽으세요 (파일 접근이 없으면 사용자에게 붙여넣어 달라고 요청)
- `1_spack.md` 를 기반으로 API 엔드포인트를 구현하세요
- 각 엔드포인트를 작성하기 전에 `skills/` 규칙을 확인하세요
- OUTPUT → API 라우트 목록 → **STOP 하고 사용자 확인을 기다리세요**

### PHASE 5 — 통합 및 검증
- ↺ 지금 `3_architecture.md` 의 connection map 섹션을 다시 읽으세요 (파일 접근이 없으면 사용자에게 붙여넣어 달라고 요청)
- connection map 을 따라 서비스를 연결하고, 기본 build/run 을 검증하세요
- OUTPUT → 빌드 상태 + 미구현 항목 목록 → **STOP 하고 사용자 확인을 기다리세요**

### PHASE 6 — 전수 대조 감사 루프 (완료 보증 — 마지막 관문)
- `IMPLEMENTATION-CHECKLIST.md` 를 여세요 — 그것은 **설계 그래프에서 기계 생성(machine-generated)** 되었으므로 빠진 것이 없습니다
- 모든(EVERY) 항목에 대해: 코드에 실제로 존재하는지 검증하세요. 존재하면 `- [x]` 로 표시하고 `←구현위치:` 뒤에 실제 파일 경로를 쓰세요
- 당신이 적는 모든 경로는 확인을 위해 실제로 **열어본(OPENED)** 파일이어야 합니다 — 열 수 없는 경로는 거짓 체크입니다(a path you cannot open is a false check)
- 실제 파일을 가리킬 수 없는 항목 = **구현 안 됨(NOT implemented)** → 그것을 구현한 뒤, 이 감사를 처음부터 다시 실행하세요
- **항목의 100% 가 체크될 때까지 이 루프를 반복하세요(Repeat this loop until 100% of items are checked).** 그 전에 프로젝트가 완료되었다고 선언하지 마세요.
- OUTPUT → 완전히 체크된 목록 + 커버리지 카운트(예: `N/N ✅`) → **STOP 하고 사용자 확인을 기다리세요**
- 감사 100% 이후: `SHIP_TO_HARNESS.md`를 열어 그 절차대로 저장소 업로드까지 **네가 직접 실행**하라 — 명령은 네가 치고, 사용자는 클릭·승인만 한다. (그 파일이 폴더에 없으면: 사용자에게 완성 코드를 GitHub에 올리도록 안내하라.) 업로드 후 사용자가 Harness 결과물(Deliverables) 화면에 저장소를 등록하고 Lint 화면에서 검증을 실행해 점수를 받도록 안내하라. 이것이 이 플랜의 진짜 마지막 단계다.

### 필수 준수 규칙
1. **각 Phase 마다 멈추세요(STOP after every Phase).** 위에 나열된 출력을 보고하세요. 사용자의 명시적 OK 없이 다음 Phase 를 시작하지 마세요.
2. **Tech stack 은 `3_architecture.md` 에서 먼저 가져옵니다.** 거기에 명시된 스택을 사용하세요. 명시되지 않은 경우에만 Phase 1 에서 옵션을 제안하고 사용자의 명시적 선택을 기다리세요 — 절대 조용히 기본값을 쓰지 마세요.
3. **요구사항을 절대 추정하지 마세요(NEVER assume requirements).** 불명확한 것은 무엇이든 → 물어보세요. 추측하고 진행하지 마세요.
4. **모든 코딩 행위 전에 skills 규칙을 확인하세요.** 코드를 작성하기 전에 관련 `skills/` 규칙이 적용되는지 다시 확인하세요. skills 규칙 위반은 허용되지 않습니다.
5. **`3_architecture.md` 가 최종 권위입니다.** 명세가 충돌할 때는 아키텍처가 이깁니다.
6. **각 Phase 시작 시 관련 spec 파일을 다시 읽으세요** (↺). 긴 대화는 컨텍스트 드리프트를 일으킵니다 — 항상 원본에서 새로 고치세요. 파일 접근이 없으면 사용자에게 붙여넣어 달라고 요청하세요.
7. **완료 = Phase 6 감사 100%.** 모든 `IMPLEMENTATION-CHECKLIST.md` 항목이 실제 파일 경로와 함께 `[x]` 로 체크됨. "아마 동작할 것"은 완료가 아닙니다 — 감사가 통과할 때까지 반복하세요.
8. **중단 후 재개하나요(Resuming after an interruption)?** `IMPLEMENTATION-CHECKLIST.md` 와(과) 모든 `_workspace/` 노트를 먼저(FIRST) 읽어 이미 완료된 것을 확인한 뒤, 첫 번째 체크 안 된 항목부터 계속하세요 — 처음부터 다시 시작하지 마세요(do not start over).

---

## 📂 받은 파일들 — 각각 뭐예요?

### 🤖 `AGENTS.md` · `CLAUDE.md` (자동 인식 파일 — 건드릴 필요 없음)
AI 도구가 폴더를 열 때 **자동으로 읽는** 지시문입니다. 덕분에 긴 프롬프트를 붙여넣지 않아도 에이전트가 무엇을 할지 압니다. (Claude Code → `CLAUDE.md`, Cursor·Codex·Antigravity·Windsurf → `AGENTS.md`)

### 📖 `0_START_HERE.md` (지금 보고 있는 이 파일)
**사람용 시작 가이드 + AI 에이전트 작업 지시**. 위 3단계 사용법과 Phase 순서·작업 규칙이 담겨 있습니다.

### 📦 `skills/` — 코딩 규칙 모음
프로젝트에서 지켜야 할 **코딩 컨벤션** 입니다. 카테고리별 하위 폴더에 `.md` 규칙이 있으며, AI 에이전트는 코드를 작성할 때 이 규칙을 반드시 따릅니다.

### 📋 `1_spack.md` — SPACK (무엇을 만들 것인가)
시스템이 제공할 **API, Entity(데이터 구조), Policy(비즈니스 규칙)** 명세입니다.
Phase 4 API 구현의 기준이 됩니다.

### 🧩 `2_ddd.md` — DDD (어떻게 동작하는가 — 도메인)
비즈니스 도메인의 **Aggregate(관련 Entity 묶음), Domain Event(상태 변화 이벤트), Service** 모델입니다.
Phase 3 도메인 구현의 기준이 됩니다.

### 🏗️ `3_architecture.md` — Architecture (어떻게 배포할 것인가)
코드를 어떤 **서비스 / 저장소** 로 쪼개서 배포할지의 청사진.
Phase 2 골격 생성의 기준이 됩니다.

### ✅ `IMPLEMENTATION-CHECKLIST.md` — 전수 대조 체크리스트
설계 그래프에서 **기계적으로 생성**된 구현 목록 (요약 과정의 누락 없음).
마지막 Phase 6 에서 AI 에이전트가 이 목록의 **모든 항목을 코드와 대조**해 전부 ✅ 가 돼야 작업이 끝납니다.

---

지금부터 **Phase 0 — 규칙 내재화** 부터 시작하세요.
